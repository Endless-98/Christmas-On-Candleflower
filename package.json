{
  "name": "christmasoncandleflower",
  "private": true,
  "version": "0.1.0",
  "engines": { "node": ">=18.0.0" },
  "scripts": {
    "dev": "npm --prefix site run dev",
    "build": "npm --prefix site run build",
    "preview": "npm --prefix site run preview",
    "start": "npm --prefix site run preview",
    "dev:server": "npm --prefix archive/server run dev",
    "start:server": "npm --prefix archive/server run start",
    "dev:all": "concurrently \"npm run dev:server\" \"npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
