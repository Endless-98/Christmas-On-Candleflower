# Copy this file to site/.env and fill in your configuration.
# Do NOT commit the real .env file.

# Google Maps Configuration (choose one method)
# For public embed (no API key needed):
VITE_MAP_QUERY=123 Candleflower St, Your City, ST 12345

# OR for API-based embed (more features, requires API key):
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
# VITE_MAP_QUERY=123 Candleflower St, Your City, ST 12345

# OR for custom embed URL:
# VITE_MAP_EMBED_URL=https://maps.google.com/maps?q=...&output=embed

# Now Playing S3 URL (required for live song updates)
# Direct S3 bucket URL with public read access
VITE_NOW_PLAYING_S3_URL=https://your-bucket-name.s3.us-east-1.amazonaws.com/current.json
